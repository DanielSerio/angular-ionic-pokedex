import { TitleKababPipe } from './title-kabab.pipe';

describe('TitleKababPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleKababPipe();
    expect(pipe).toBeTruthy();
  });
});
